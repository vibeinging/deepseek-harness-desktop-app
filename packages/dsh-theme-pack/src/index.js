/** Theme data is projected from themes.json; this Profile-owned plugin needs no host service. */
export const name = "dsh-theme-pack";

/** Keep the theme package in the Cordis plugin tree so Profile owns its lifecycle. */
export function apply() {}
